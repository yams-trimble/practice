apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp
  namespace: arg
spec:
  project: default

  source:
    repoURL: https://github.com/yams-trimble/practice
    targetRevision: HEAD
    path: k8s

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true